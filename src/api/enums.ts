/**
 * Identifiers for queries.
 */
export enum QueryKeys {
  /**
   * Get orders.
   */
  Orders = "orders",
  /**
   * Get instruments.
   */
  Instruments = "instruments",
}

export enum OrderActionType {
  Buy = "buy",
  Sell = "sell",
}
